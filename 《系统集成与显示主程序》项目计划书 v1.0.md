
---

# 《系统集成与显示主程序》项目计划书 (v1.0)

**版本**：1.0  
**日期**：2026-07-07  
**作者**：[你的名字]  
**关联文档**：  
- 《屏幕感知模块》项目计划书 v3.0  
- 《GTO策略引擎模块》项目计划书 v3.0  

---

## 1. 项目定位

本模块是整个系统的 **集成中枢与用户界面**，负责：

- **调度与管理**：控制屏幕感知模块和GTO策略引擎模块的生命周期，协调它们之间的数据流。
- **UI呈现**：将策略建议、牌局信息、对手数据以独立窗口形式显示在第二块屏幕上。
- **部署与配置**：提供统一的配置文件、启动脚本，让整套系统可以一键运行。

**一句话概括**：让“眼睛”（感知模块）和“大脑”（引擎模块）协同工作，并把结果“说”给用户听。

---

## 2. 系统整体架构

```
┌─────────────────────────────────────────────────────────┐
│                   系统集成主程序                          │
│                                                         │
│  ┌──────────────────┐      ┌──────────────────┐        │
│  │   屏幕感知模块     │      │  GTO策略引擎模块   │        │
│  │  (子线程运行)      │      │  (Python绑定/HTTP) │        │
│  │                   │      │                   │        │
│  │  采集 → 识别 →    │      │  查询 → 策略返回   │        │
│  │  game_snapshot    │      │                   │        │
│  └────────┬─────────┘      └────────┬──────────┘        │
│           │  game_snapshot 字典      │ 查询结果JSON        │
│           └──────────┬──────────────┘                    │
│                      ▼                                   │
│           ┌─────────────────────┐                        │
│           │   主调度控制器        │                        │
│           │  ·事件循环            │                        │
│           │  ·牌局状态机          │                        │
│           │  ·请求构建与分发      │                        │
│           └──────────┬──────────┘                        │
│                      │                                   │
│           ┌──────────▼──────────┐                        │
│           │   用户界面 (GUI)     │                        │
│           │  ·策略建议窗口       │                        │
│           │  ·对手统计面板       │                        │
│           │  ·牌局信息回显       │                        │
│           │  ·配置设置界面       │                        │
│           └─────────────────────┘                        │
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │              配置与部署层                         │    │
│  │  config.yaml / 启动脚本 / 依赖管理 / 文件结构     │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 主调度控制器设计

### 3.1 核心事件循环

```python
def main_loop():
    # 1. 加载配置
    cfg = load_config("config.yaml")

    # 2. 初始化视频源（根据配置选择采集卡/NDI/本机抓屏）
    video_source = create_video_source(cfg["capture"])

    # 3. 初始化感知模块（传入 roi 等配置和视频源）
    perception = PerceptionModule(cfg["capture"], video_source)

    # 4. 初始化引擎模块
    gto_engine.init(cfg["engine"])

    # 5. 初始化状态机与UI
    state_machine = GameStateMachine()
    ui = MainWindow(cfg["ui"])

    # 6. 启动感知处理线程
    perception_thread = Thread(target=perception_loop, args=(perception,))
    perception_thread.start()

    # 7. 主循环
    while True:
        # 从感知模块获取最新快照（非阻塞）
        snapshot = perception.get_latest_snapshot()

        # 更新状态机
        event = state_machine.update(snapshot)

        # 当轮到我们行动且状态变化时，请求策略
        if event == "MY_TURN" and state_machine.has_changed():
            query = state_machine.build_query()
            result = gto_engine.query(query)
            ui.show_strategy(result)

        # 更新牌局信息回显
        ui.update_game_info(state_machine.get_current_state())

        # 处理UI事件
        ui.process_events()

        # 控制循环频率
        time.sleep(0.05)
```

### 3.2 牌局状态机职责

`GameStateMachine` 由主程序维护，它负责：

- 接收 `game_snapshot`，追踪当前牌局阶段（翻前/翻牌/转牌/河牌）。
- 检测是否轮到我们行动。
- 根据 `dealer_seat` 和 `my_seat` 自动计算位置字符串（BTN、BB 等）。
- 将 `actions` 字段累积为标准 `history` 字符串（如 `r200:c:b500`）。
- 构建符合 v1.0 协议的查询 JSON。

### 3.3 与引擎的交互

- 主程序通过 `gto_engine.query()` 直接调用 Python 绑定（同进程），或通过 HTTP 请求调用独立服务。
- 引擎返回的 JSON 直接交给 UI 层解析显示。

---

## 4. 用户界面设计

策略建议显示在**第二块屏幕的独立窗口**中，布局如下：

```
┌─────────────────────────────────────────┐
│  GTO Assistant                        │
├─────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────────┐  │
│  │  你的手牌    │  │   公共牌         │  │
│  │  A♥ K♥      │  │  A♠ 7♦ 2♥      │  │
│  └─────────────┘  └─────────────────┘  │
│  ┌─────────────────────────────────────┐│
│  │          策略建议                    ││
│  │  ● 下注 33% 底池    ████████░░ 85% ││
│  │     EV: +12.5 BB                    ││
│  │  ○ 过牌             ██░░░░░░░░ 15% ││
│  │     EV: +11.2 BB                    ││
│  │  【当前推荐: 下注33%】              ││
│  └─────────────────────────────────────┘│
│  ┌─────────────────────────────────────┐│
│  │  牌局信息                           ││
│  │  位置: BTN | 底池: 1500 | 筹码: 8500││
│  │  历史: r200:c                       ││
│  └─────────────────────────────────────┘│
│  ┌─────────────────────────────────────┐│
│  │  对手统计 (从数据库加载)             ││
│  │  座位1 (UTG): VPIP 24% PFR 18%     ││
│  └─────────────────────────────────────┘│
│  [设置] [牌谱记录] [模式: GTO]         │
└─────────────────────────────────────────┘
```

**面板说明**：
- 手牌/公共牌回显：让用户确认感知结果准确。
- 策略建议区：动作频率条、EV、高亮推荐。
- 牌局信息：位置、底池、筹码、行动历史。
- 对手统计：从本地 `opponents.db` 读取。
- 底部控制栏：打开设置、开关牌谱保存、切换 GTO/Exploit 模式（Exploit 模式目前仍返回 GTO 策略，后期扩展）。

---

## 5. 配置与部署

### 5.1 统一配置文件 (`config.yaml`)

所有模块的配置集中在此文件，由主程序读取并分发给各模块。

```yaml
capture:
  method: "screen_grab"       # capture_card / ndi / screen_grab
  device_id: 0                # 采集卡专用
  ndi_source: "GAME-PC"       # NDI 专用
  monitor_index: 1            # 抓屏专用
  fps: 20
  roi:                        # 由标定工具生成
    hand_card_1: [100, 200, 50, 70]
    # ... 其他ROI

engine:
  mode: "python_binding"      # python_binding 或 http
  data_path: "./engine/data/"
  index_file: "./engine/data/index.bin"
  http_url: "http://127.0.0.1:9090/query"  # 仅 http 模式

ui:
  display_screen: 1           # 0=主屏, 1=副屏
  window_width: 400
  window_height: 700
  always_on_top: true
  opacity: 0.95

hand_history:
  save_enabled: true
  save_preflop: true
  save_flop: true
  save_turn: true
  save_river: true
  save_path: "./data/hand_histories/"

opponent_tracking:
  enabled: true
  database_path: "./data/opponents.db"
```

### 5.2 完整文件结构

```
gto_assistant/
├── main.py                  # 主程序入口
├── config.yaml
├── requirements.txt
├── start.bat                # Windows 一键启动
├── perception/              # 感知模块
│   ├── module.py
│   ├── recognition/
│   └── models/
├── engine/                  # 引擎模块
│   ├── gto_engine.pyd
│   ├── engine_server.exe (可选)
│   └── data/
├── ui/                      # 界面模块
│   ├── main_window.py
│   ├── strategy_panel.py
│   ├── opponent_panel.py
│   └── settings_dialog.py
├── data/                    # 用户数据
│   ├── opponents.db
│   └── hand_histories/
└── logs/
```

### 5.3 依赖清单 (`requirements.txt`)

```
PyQt5>=5.15
opencv-python>=4.8
numpy>=1.24
mss>=9.0
paddleocr>=2.7
torch>=2.0
requests>=2.31
pyyaml>=6.0
```

---

## 6. 开发计划

| 阶段 | 任务 | 预计时间 | 产出 |
|------|------|---------|------|
| **一：骨架搭建** | 配置文件解析、创建主循环、加载模块 mock | 1 周 | 可启动的空主程序 |
| **二：状态机与数据流** | 实现 GameStateMachine，打通 感知→状态机→引擎 数据链路 | 1 周 | 数据流通 |
| **三：GUI 开发** | 实现主窗口、策略面板、牌局信息、对手统计 | 2 周 | 完整显示界面 |
| **四：高级功能** | 设置界面、牌谱保存、对手统计、模式切换 | 1 周 | 完整用户交互 |
| **五：集成测试** | 替换 mock，与感知、引擎模块联调，端到端测试 | 1 周 | 稳定可运行系统 |
| **总计** | | **约 6 周** | |

---

## 7. 预算

- 硬件：已有双电脑 / 采集卡（可选）
- 软件：全部开源或已有
- **总计：0 额外软件成本**

---

## 8. 风险与应对

| 风险 | 应对 |
|------|------|
| 模块间接口不一致 | 严格遵循本文档中的快照字典、查询 JSON 格式，并使用 mock 提前联调 |
| 感知延迟导致策略滞后 | 可通过配置文件降低 fps，优先保证识别准确率；UI 在等待时显示“识别中” |
| 引擎未命中 | 引擎返回 `approx` 状态，UI 上明确标注“近似值” |
| 副屏窗口被遮挡 | 设置 `always_on_top: true` 确保始终可见 |

---

**项目体系完整度**：

- ✅ 屏幕感知模块 v3.0
- ✅ GTO策略引擎模块 v2.0
- ✅ 系统集成与显示主程序 v1.0
