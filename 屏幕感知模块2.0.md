

# 《屏幕感知模块》项目计划书 (v2.0)

**版本**：2.0  
**日期**：2026-07-07  
**作者**：[你的名字]

---

## 1. 项目定位与目标

本项目是整个GTO辅助系统的 **“眼睛”** ，负责从**另一台设备（或本机）** 上运行的 **WePoker 9人桌** 固定布局牌局中，实时、准确地提取结构化牌局信息。它将视频画面转化为标准数据，供GTO策略引擎模块查询。

**核心职责**：
- **看**：实时截取并识别牌桌上的所有关键元素。
- **懂**：将识别结果整合成包含行动序列、位置、筹码量的完整牌局状态。
- **记**：按需保存牌谱，用于复盘。
- **传**：将标准化的JSON请求发送给策略引擎。

---

## 2. 技术架构

系统支持**双机**和**单机**两种物理部署模式，但软件架构保持一致，通过统一的**视频采集接口**屏蔽底层差异。

```
┌─── 游戏设备 ───┐        ┌─── 感知设备 (R5 5600, 32G, RTX3060) ───┐
│  (运行WePoker)  │        │                                          │
│                 │ 视频流 │  ┌────────────────────────────────────┐  │
│  [双机方案] ────┼───────┼─>│     统一视频采集接口 (抽象层)       │  │
│  采集卡/NDI/虚拟│        │  │  具体实现: 采集卡/NDI/屏幕抓取等   │  │
└─────────────────┘        │  └────────────────┬───────────────────┘  │
                           │                   ▼                      │
                           │  ┌────────────────────────────────────┐  │
                           │  │          牌局感知引擎               │  │
                           │  │  ·牌面匹配/分类 ·动作检测 ·数字OCR  │  │
                           │  └────────────────┬───────────────────┘  │
                           │                   ▼                      │
                           │  ┌────────────────────────────────────┐  │
                           │  │       牌局状态机与记录服务          │  │
                           │  │  状态追踪 → 构造JSON → 发往引擎    │  │
                           │  └────────────────────────────────────┘  │
                           │                    │                     │
                           │           局域网/本地进程间               │
                           │                    ▼                     │
                           │         ┌──────────────────┐             │
                           │         │  GTO 策略引擎模块 │             │
                           │         └──────────────────┘             │
                           └──────────────────────────────────────────┘
```

---

## 3. 视频采集与传输方案（支持本机+双机）

我们将视频源抽象为一个统一的 `VideoCapture` 接口，让你可以像换插件一样切换方案。下面列出所有支持的方案，包括本机操作。

### 方案对比总览

| 方案 | 适用环境 | 延迟 | 成本 | 传输/调用接口 |
|------|---------|------|------|------|
| **1. HDMI采集卡（推荐双机）** | 双机 | **极低 (<50ms)** | ￥100-500 | 标准 `USB 摄像头` 协议，OpenCV 直接读取 |
| **2. OBS + NDI 网络串流** | 双机 / 本机模拟 | 中 (100-300ms) | 免费 | `ndi-python` 库，通过网络IP拉流 |
| **3. OBS 虚拟摄像头** | 双机(差)/本机扩展屏 | 高 (>200ms) | 免费 | 标准 `USB 摄像头` 协议，需先在OBS设置输出 |
| **4. 本机屏幕抓取（推荐单机）** | **单机** | **极低 (<30ms)** | 免费 | `mss` 或 `DXGI` 直接抓屏，性能最好 |

---

### 方案详情与接口实现

#### 方案 1：HDMI 硬件采集卡（双机，推荐）
- **物理连接**：游戏电脑HDMI OUT → 采集卡 → 感知电脑USB IN。
- **传输接口与调用**：系统将采集卡识别为普通USB摄像头。Python代码用OpenCV直接打开，就像开一个笔记本自带摄像头一样。
  ```python
  import cv2
  cap = cv2.VideoCapture(0) # 采集卡通常就是摄像头0
  ```
- **优点**：独立硬件传输，不占网络和CPU，延迟极低，画面无损。
- **硬件推荐**：MS2130芯片方案的USB 3.0采集卡（￥100-200）。

#### 方案 2：OBS + NDI 网络串流（双机零成本备选）
- **物理连接**：两台电脑接同一个局域网。
- **传输接口与调用**：使用 `ndi-python` 库通过网络接收NDI流。
  ```python
  import ndi
  # 需要在游戏中电脑用OBS+NDI插件输出，感知电脑用此代码接收
  ```
- **优点**：完全免费，无线或局域网。
- **缺点**：延迟受网络质量影响，图像有压缩，不适合精确识别小花色。

#### 方案 3：OBS 虚拟摄像头（单机/双机备选）
- **物理连接**：在游戏电脑装OBS，开启“虚拟摄像头”输出。
- **传输接口与调用**：同样是标准摄像头协议，双机时用USB网络摄像头软件（如DroidCam）中转；单机时可直接被Python程序捕获。
  ```python
  cap = cv2.VideoCapture(1)  # OBS虚拟摄像头设备号
  ```
- **缺点**：单机用来模拟双机流程可以，但本身没降低延迟；双机用它依赖第三方软件，不如采集卡稳定。

#### 方案 4：本机屏幕抓取（单机新方案）
- **物理连接**：无需额外硬件，游戏和感知程序跑在同一台电脑上。
- **传输接口与调用**：使用 `mss` 库直接抓取屏幕/窗口画面，效率极高。
  ```python
  import mss
  import numpy as np
  with mss.mss() as sct:
      monitor = sct.monitors[1] # 抓副屏或主屏
      img = np.array(sct.grab(monitor))
  ```
- **优点**：性能最好的方案，延迟最低，零成本。完全满足你“本机也可以操作”的需求。

---

### 统一的采集接口设计

为了后期能随意切换上述任一方案，我们把采集逻辑封装起来：

```python
from abc import ABC, abstractmethod

class VideoSource(ABC):
    """统一的视频采集接口"""
    @abstractmethod
    def get_frame(self) -> np.ndarray:
        """返回一帧图像 (H x W x C, BGR格式)"""
        pass

class CaptureCardSource(VideoSource):
    # 实现方案1
    def get_frame(self): ...

class NDISource(VideoSource):
    # 实现方案2
    def get_frame(self): ...

class LocalScreenSource(VideoSource):
    # 实现方案4，推荐单机
    def get_frame(self): ...
```

这样，主程序只依赖 `VideoSource` 接口，切换方案时只需修改一行初始化代码，其余所有识别逻辑完全不变。

---

## 4. 与 GTO 策略引擎的接口设计

感知模块的最终价值是把“看到”的画面，变成策略引擎能听懂的请求，并接收建议。

### 4.1 发送请求至策略引擎

我们将严格按照《GTO策略引擎模块》约定的 **v1.0 协议** 构造JSON请求。

**请求格式**：
```json
{
  "version": "1.0",
  "hand": "AhKh",
  "board": "As7d2h",
  "history": "r200:c",
  "position": "BTN",
  "pot": 1500,
  "stack": 8500,
  "options": {
    "include_ev": true
  }
}
```

**调用接口**：策略引擎模块提供了多种调用方式，感知模块可根据部署情况选择：
- **本机单进程（最简单）**：直接 `import gto_engine` 调用Python绑定。
- **本机多进程/双机**：向策略引擎启动的 **本地HTTP服务** 发POST请求。
  ```python
  import requests
  response = requests.post("http://127.0.0.1:9090/query", json=request_json)
  ```

### 4.2 接收并处理响应

引擎会返回标准响应，包含各动作的推荐频率和EV。

**响应格式**：
```json
{
  "status": "ok",
  "actions": [
    { "type": "bet", "size": 0.33, "freq": 0.85, "ev": 12.5 },
    { "type": "check", "freq": 0.15, "ev": 11.2 }
  ],
  "recommendation": "bet33",
  "is_exact": true
}
```

**错误与降级处理**：
- 当识别置信度低时，请求中加 `"confidence": "low"`，引擎会返回带近似标记的结果，而不是拒绝服务。
- 如果引擎未命中，会返回 `"status": "not_found"`，感知模块应在UI提示用户“当前场景无数据”。

---

## 5. 详细功能规划

### 5.1 核心信息提取

| 目标信息 | 技术方案 | 说明 |
|---------|---------|------|
| **自己的底牌** | 高精度模板匹配（主方案） / 小型CNN分类（备用） | 固定位置，优先分别识别点数和花色，再组合成标准牌面 |
| **公共牌** | 同上，动态监测区域变化 | 逐步识别翻牌3张、转牌1张、河牌1张，不使用OCR识别扑克牌本身 |
| **对手动作与下注额** | 按钮状态变化检测 + OCR识别金额 | 检测“过牌/下注/加注/弃牌”按钮是否亮起，并读取下注框或动作文字旁的数字。**需用户自行调整抓帧间隔以达到最佳准确率** |
| **底池与筹码量** | OCR识别固定区域的数字 | 定期读取总底池和每位玩家的筹码数字；OCR仅用于数字区域 |
| **玩家位置** | 识别庄位按钮（Dealer Button）图像 | 通过识别D按钮的位置，推断所有玩家的相对位置 |
| **行动权** | 检测高亮框/时间条/按钮状态 | 识别轮到谁行动，这是触发策略请求的关键信号 |

### 5.2 牌局状态机（核心逻辑）
- **状态维护**：维护一手牌从“Preflop”到“River”的完整生命周期。
- **行动序列构建**：将每次检测到的动作自动拼接成标准历史字符串（如 `r200:c`）。
- **状态重置**：牌局结束时（检测到“庄位移动”或新一轮发牌），自动重置并选择性地保存上一手牌谱。

### 5.3 对手数据与策略选择
- **对手记录**：允许用户为一个座位上捕获的头像或昵称创建一个匿名档案。
- **个人统计**：为该档案累积记录VPIP（入池率）、PFR（翻前加注率）等基础数据。
- **双模策略接口**：在向策略引擎发送请求时，可附加对手ID或统计信息，并标记请求模式为：
  - `"mode": "GTO"` （纯GTO策略，不考虑对手倾向）
  - `"mode": "Exploit"` （请求结合对手倾向的剥削策略，需策略引擎模块支持）

### 5.4 牌谱记录与复盘
- **选择性保存**：提供复选框，让用户选择保存“翻前、翻后、转牌、河牌”各阶段的具体信息。
- **标准化输出**：将识别的完整牌局保存为可读的JSON或文本格式，便于导入复盘工具或策略引擎进行分析。
- **不保存视频**：严格按你的要求，只存储结构化数据，极大节省硬盘空间。

---

## 6. 性能与精度目标
- **端到端延迟**：从画面动作完成到生成请求，目标控制在 **500ms 以内**。提供可配置的抓帧间隔，让你在准确率和响应速度间自行调整。
- **识别精度（初期）**：容忍 **< 10%** 的单次识别错误率（尤其花色），后期通过模型迭代和数据增强不断优化。
- **降级策略**：当识别置信度低时，在发往策略引擎的请求中附加 `"confidence": "low"` 标记，由策略引擎返回带“不确定性”标记的近似建议，而不是直接报错。

---

## 7. 开发计划

| 阶段 | 任务 | 预计时间 | 产出 |
|------|------|---------|------|
| **一：环境搭建** | 选择一种采集方案（推荐单机mss或双机采集卡），标定所有ROI，验证稳定性。 | 1周 | 稳定采集并裁剪9人桌各区域图像 |
| **二：核心识别模块** | 开发底牌/公共牌模板识别器（必要时再补CNN）；开发动作/按钮状态检测器。 | 3周 | 可独立识别牌面、动作、轮次 |
| **三：数字OCR与状态机** | 集成PaddleOCR读取筹码/底池/下注额；构建完整牌局状态机。 | 2周 | 可输出完整一手牌的结构化日志 |
| **四：接口对接** | 实现统一采集接口，封装向策略引擎的JSON请求，开发牌谱保存功能。 | 1周 | 可与策略引擎完成闭环，并保存牌谱 |
| **五：集成测试** | 在模拟币环境试运行，调整抓帧间隔，优化识别准确率，测试降级策略。 | 1周 | 稳定运行的 Beta 版本 |
| **总计** | | **约 8 周** | |

---

## 8. 预算与硬件

| 项目 | 说明 | 预算 |
|------|------|------|
| **HDMI采集卡 (可选)** | 用于双机低延迟方案 | ￥100-500 |
| **硬件** | 你已有的 R5 5600 + 32G + RTX3060 | **0** |
| **软件库** | OpenCV, mss, PaddleOCR（数字区域）, PyTorch（仅CNN备用方案需要），均为开源 | **0** |
| **总计** | | **￥0 – ￥500（一次性硬件）** |

---

## 9. 风险与应对

| 风险 | 应对 |
|------|------|
| **WePoker界面更新导致ROI失效** | 设计简单的ROI重标定工具，可在几分钟内重新配置 |
| **遮挡或反光导致识别失败** | 设计`confidence`标记与降级策略；后期模型训练加入带噪声/遮挡的样本 |
| **高识别错误率影响决策** | 提供抓帧频率调节选项，牺牲一点速度换取多次采样确认；后期持续优化模型 |
| **双机方案延迟过高** | 优先选用采集卡或本机mss方案；若用NDI，确保有线网络连接 |
| **单机运行性能压力** | 你的R5 5600 + RTX3060足够；感知程序可限制在4核内运行，不影响游戏 |

---

## 10. 合规声明

本项目**严格限定用于模拟币/训练平台或个人学术研究**。使用者需自行确保不违反任何游戏平台的用户协议。项目文档中应显著位置声明禁止用于真钱对局。

---

如果还有需要细化的地方，或者想开始动手搭建，我可以继续协助。
