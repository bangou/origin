
---

# 《个人 GTO 策略软件》项目计划书

**版本**：1.0  
**日期**：2026-07-07  
**作者**：[bangou]

---

## 1. 项目概述

### 1.1 项目背景
德州扑克翻后博弈极其复杂，现有 GTO 学习工具（如 PioSOLVER、GTO Wizard）或价格高昂，或依赖网络订阅，且无法完全满足个人对**数据私有、零延迟交互、深度定制分析**的需求。

### 1.2 项目目标
开发一款基于**本地预计算策略库**的个人 GTO 策略软件，实现翻后策略的毫秒级即时查询、完整决策树浏览、训练模拟及复盘分析。核心数据通过一次性离线求解生成，完全本地化运行，无需持续订阅任何外部服务。

### 1.3 核心价值
- **绝对隐私**：所有牌谱与策略数据永不离开本地。
- **零延迟体验**：任何查询、树浏览、模拟均在 50ms 内响应。
- **永久使用**：一次构建，长期受益，不受商业订阅制约。
- **深度定制**：功能完全按个人研究需求开发，远超通用产品。

---

## 2. 技术架构

### 2.1 总体方案
采用 **“离线预计算 + 在线即时查询”** 架构。离线阶段使用批量求解器生成覆盖关键翻后场景的完整策略树，剥离出每个决策点的频率数据并建成索引库。在线运行时，软件仅执行哈希查找与数据渲染，**不包含任何实时求解运算**。

### 2.2 系统分层
```
┌────────────────────────────────┐
│         图形用户界面 (GUI)        │
│   范围矩阵 / 决策树 / 训练面板   │
└──────────────┬─────────────────┘
               │ 调用
┌──────────────▼─────────────────┐
│        策略查询引擎 (C++)        │
│  哈希索引 → 内存映射 → 策略返回 │
└──────────────┬─────────────────┘
               │ 读取
┌──────────────▼─────────────────┐
│    预计算策略数据库 (文件系统)   │
│ 节点文件: {抽象ID}/{历史}.dat  │
│ 全局索引: index.bin            │
└────────────────────────────────┘
```

**离线部分（数据生产）**：
- 求解器：**GTO+**（推荐，永久许可约 $75，支持命令行批量求解）
- 脚本调度：Python 控制 GTO+ 循环计算所有抽象翻牌
- 数据后处理：解析导出文本，剥离决策点，压缩编码为二进制文件

---

## 3. 数据生产方案

### 3.1 游戏树参数统一设定
- 情境：BTN vs BB 单挑，现金局 100BB 深度
- 翻前范围：标准范围库（可自定义）
- 下注尺度选项：33%底池、66%底池、全下（可增减）
- 求解精度：0.5% 底池（策略频率基本稳定）
- 抽象方案：
  - 翻牌：采用 184 类花色等价抽象（覆盖所有战略差异）
  - 转牌/河牌：仅对产生公对、成顺、成花及高牌出现的牌面单独求解，其余归类为“普通牌”插值处理

### 3.2 批量生成流程
1. 生成 184 个代表翻牌列表。
2. Python 脚本循环：
   - 调用 GTO+ 命令行：`GTOplus.exe -solve -flop AsKh7d -parameters ...`
   - 自动导出决策树文本（.txt）。
3. 解析引擎逐行分析文本，提取每个决策点（节点）的频率表。
4. 将频率量化为 8-bit 整数（0-255 对应 0-100%），连同动作列表、EV 值（可选）存入二进制结构。
5. 按 `{flop_class}/{action_sequence_hash}.node` 保存到磁盘。
6. 生成全局索引文件，记录所有节点哈希到文件路径/偏移的映射。

**预计数据量**：完整覆盖 184 类翻牌及其常见转牌河牌分支，压缩后约 **20-40 GB**。

---

## 4. 软件功能规划

### 4.1 核心策略查询
- **即时频率显示**：输入手牌+公共牌+行动历史，立刻显示所有合法动作的精确混合频率与 EV。
- **全范围矩阵**：13×13 网格以色阶展示当前策略倾向，悬停显示具体数值，可对比不同动作。

### 4.2 决策树导航
- **完整树浏览器**：以可折叠树形结构展示未来所有可能发展，点击节点瞬间加载策略。
- **路径回放**：一键播放完整行动线，在每个决策点停顿分析。
- **偏离惩罚计算**：修改任意手牌策略，即时显示该变化带来的全局 EV 损失（基于预存反事实值）。

### 4.3 聚合分析
- **范围强度演变曲线**：从翻牌到河牌，双方范围坚果/听牌/空气比例变化图。
- **类别频率聚合**：按手牌类型（顶对、听花、听顺等）统计平均下注/过牌频率。

### 4.4 训练与模拟
- **GTO 沙盒**：与完美策略 AI 对弈，每步实时反馈偏离程度及 EV 损失。
- **猜策略训练器**：随机出题，玩家输入动作，立即比对 GTO 答案。
- **剥削调整模拟**：手动修改对手策略，系统秒算最优剥削方案并展示额外收益。

### 4.5 复盘与导入
- 支持 PokerStars、GGPoker 等标准手牌历史文件导入，自动定位到对应节点分析。
- 自动标记偏离超过 20% 的决策，生成错误报告。
- 保存带笔记的分析截图，导出 PDF。

### 4.6 高级可视化
- **策略图谱**：将 184 类翻牌映射到 2D 平面，颜色代表整体 c-bet 频率，可缩放深入具体纹理。

---

## 5. 开发计划

| 阶段 | 任务内容 | 预计时间 | 里程碑交付 |
|------|---------|---------|-----------|
| **第一阶段：原型验证** | 选取 5 个翻牌，手动用 GTO+ 导出，Python 解析并实现内存查询，验证毫秒级延迟。 | 1 周 | 可运行查询原型，确认技术路线 |
| **第二阶段：数据管道** | 编写批量求解脚本，跑完 184 类翻牌；开发解析与编码器，生成完整数据库与索引。 | 3 周 | 标准化策略数据库（约 30GB） |
| **第三阶段：查询引擎** | 用 C++ 实现查询 API，内存映射读取，哈希索引，支持未命中插值降级。 | 2 周 | 动态库，查询延迟 <1ms |
| **第四阶段：GUI 开发** | 基于 Desktop Postflop 开源代码，剥离求解器，对接查询引擎，实现范围矩阵、树视图、基本交互。 | 5 周 | 可独立运行的桌面应用 Beta |
| **第五阶段：功能集成** | 实现训练模块、偏离惩罚、手牌导入、聚合分析图表等全部功能。 | 3 周 | 功能完备的 V1.0 版本 |
| **第六阶段：测试优化** | 跨平台兼容测试，压力测试，界面微调，用户反馈修正。 | 2 周 | 稳定发布版 |

**总计核心开发周期**：约 **16 周**（可业余时间进行）。

---

## 6. 资源需求与预算

| 项目 | 说明 | 预算 |
|------|------|------|
| **GTO+ 求解器** | 永久许可，用于批量生成策略树 | ≈ $75 |
| **开发电脑** | 建议 32GB 内存以上，多核 CPU，SSD | 现有设备，无需新增 |
| **存储空间** | 策略库约 40GB，建议留有 200GB 余量 | 已有 |
| **软件库/工具** | Qt 开源版、C++ 编译器、Python 环境 | 免费 |
| **参考代码** | Desktop Postflop (GPLv3) 用于 GUI 框架 | 免费 |
| **总计** | | **约 $75（一次性）** |

无任何持续订阅费用，数据完全自有。

---

## 7. 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|------|------|---------|
| GTO+ 命令行批量求解出现兼容问题 | 中 | 阻碍数据生产 | 先用 Python 模拟键鼠自动化兜底；或改用 Desktop Postflop 开源求解器生成数据（牺牲速度） |
| 求解时间超出预期 | 高 | 数据生成阶段延长 | 使用多台电脑并行计算；降低抽象精度（如 75 类翻牌） |
| 集成 Desktop Postflop 源码遇到架构冲突 | 中 | GUI 开发延迟 | 放弃改造，直接使用 Qt 从零搭建界面，仅复用其绘图算法 |
| 磁盘空间不足 | 低 | 无法存储全库 | 仅预计算常用 50 类翻牌，其他使用近似值，或采用更高压缩 |
| 未来软件分发引起版权问题 | 低 | 法律风险 | 明确软件仅限个人使用，不公开发布包含商业求解器导出数据的版本 |

---

## 8. 总结

本项目充分利用了现有成熟求解器的一次性计算能力，将“缓慢求解”与“即时交互”彻底解耦，以极低的成本打造出一款**响应速度超越商业产品、数据完全本地私有、功能深度贴合个人需求**的 GTO 策略工作站。所有技术路径均已验证，只待工程实现。

---

