
---

# 《屏幕感知模块》项目计划书 (v3.0)

**版本**：3.0  
**日期**：2026-07-07  
**作者**：[你的名字]  
**关联文档**：《系统集成与显示主程序》v1.0

---

## 1. 项目定位

本模块是整个系统的 **“眼睛”** ，负责从 **WePoker 固定布局 9人桌** 画面中实时提取结构化牌局信息。它被主程序以子模块形式调用，**只负责图像识别与状态输出**，不直接与策略引擎或UI通信。

**核心约定**：
- **输入**：由主程序传入的配置字典（来自 `config.yaml` 的 `capture` 及 `roi` 部分）和已初始化的 `VideoSource` 对象。
- **输出**：一个标准的 `game_snapshot` 字典，供主程序的调度控制器使用。
- **职责边界**：不负责牌局状态机、不负责对手数据库写入、不负责向引擎发送请求。

---

## 2. 与主程序的接口协议

### 2.1 输入：配置与采集源

主程序在初始化感知模块时，会传入一个 `config` 字典和已准备好的 `video_source` 对象。

**config 字典示例**：
```python
config = {
    "roi": {
        "hand_card_1": [100, 200, 50, 70],
        "hand_card_2": [160, 200, 50, 70],
        "board_cards": [
            [300, 200, 50, 70], [360, 200, 50, 70], [420, 200, 50, 70],
            [480, 200, 50, 70], [540, 200, 50, 70]
        ],
        "pot_area": [400, 300, 120, 30],
        "my_chips_area": [400, 400, 120, 30],
        # ... 其他ROI（各玩家动作区域、筹码区域、庄位按钮等）
    },
    "fps": 20,
    "confidence_threshold": 0.7
}
```
- **roi**：所有兴趣区域坐标，由用户通过主程序的标定工具生成，保存在主配置文件中。
- **fps**：感知模块内部处理帧率，由主程序统一控制。

**video_source 对象**：
- 必须是一个遵循 `VideoSource` 接口的实例（定义在主程序的采集抽象层中），已由主程序根据 `capture.method` 初始化好。
- 感知模块只需调用 `video_source.get_frame()` 获取最新帧，无需关心采集细节。

### 2.2 输出：标准牌局快照 (game_snapshot)

感知模块每次识别完成后，通过 `get_latest_snapshot()` 方法向主程序返回一个字典。

**game_snapshot 字典结构**：
```json
{
  "timestamp": 1690000000.123,
  "hand": ["Ah", "Kh"],
  "board": ["As", "7d", "2h"],
  "actions": {
    "my_last_action": {"type": "bet", "size": 200},
    "opponent_actions": [
      {"seat": 3, "type": "call"},
      {"seat": 5, "type": "fold"}
    ]
  },
  "pot": 1500,
  "my_stack": 8500,
  "my_seat": 2,
  "dealer_seat": 8,
  "current_turn_seat": 2,
  "confidence": {
    "hand": 0.95,
    "board": 0.98,
    "pot": 0.92
  }
}
```

**字段说明**：
- `hand`：自己的两张手牌列表，花色点数标准格式（如 `"Ah"`）。
- `board`：当前公共牌列表（0、3、4 或 5 张）。
- `actions`：
  - `my_last_action`：上一轮我的动作及下注额。
  - `opponent_actions`：最近一轮对手的动作序列，包含座位号和动作类型。
- `pot`：当前底池大小。
- `my_stack`：我的剩余筹码量。
- `my_seat`：我的座位号（0-8）。
- `dealer_seat`：庄位按钮所在座位号。
- `current_turn_seat`：当前行动权的座位号。
- `confidence`：关键信息的识别置信度（0-1），主程序可根据此值决定是否使用降级策略。

**注意事项**：
- 主程序的**牌局状态机**会根据 `dealer_seat` 和 `my_seat` 自动计算位置（BTN/BB等）。
- 主程序根据 `actions` 字段自动拼接 `history` 字符串（如 `r200:c`），感知模块不负责此项工作。
- 若识别失败，`confidence` 可低于阈值，但仍需返回尽可能多的信息，主程序将决定后续处理。

---

## 3. 技术实现设计

### 3.1 主入口类

感知模块对外暴露一个统一的 Python 类，供主程序实例化。

```python
class PerceptionModule:
    def __init__(self, config: dict, video_source: VideoSource):
        """初始化识别器、加载模型等"""
        pass

    def get_latest_snapshot(self) -> dict:
        """非阻塞返回最新的 game_snapshot 字典。
        若自上次调用后无新帧处理完成，则返回上次快照。"""
        pass

    def update(self):
        """由主程序在独立线程中循环调用，执行一帧的采集与识别处理。"""
        pass

    def shutdown(self):
        """释放资源"""
        pass
```

- 主程序可选择在子线程中循环调用 `update()`，或由模块内部自行开启处理线程（推荐由主程序管理线程生命周期）。
- `get_latest_snapshot()` 是线程安全的，可随时被主线程调用。

### 3.2 内部识别流水线（不变）

识别算法与 v2.0 保持同一方向，但进一步明确主次：
- **牌面识别（主方案）**：固定 ROI + 图像预处理 + 高精度模板匹配，分别识别点数与花色后再组合成标准牌面。
- **牌面识别（备用方案）**：当模板匹配在缩放、抗锯齿或主题变化下不稳定时，再升级为小型 CNN 分类器。
- **动作检测**：按钮状态变化检测 + 下注额数字识别。
- **筹码/底池**：PaddleOCR 仅用于固定区域数字，不参与扑克牌本身识别。
- **位置与行动权**：识别庄位按钮图像、高亮框/时间条。

识别结果直接填充到标准 `game_snapshot` 字典中。

---

## 4. 部署与文件结构

在整体项目的 `perception/` 目录下：

```
perception/
├── __init__.py
├── module.py              # PerceptionModule 主类
├── recognition/
│   ├── card_classifier.py
│   ├── action_detector.py
│   └── ocr_reader.py
├── models/
│   └── card_cnn.pth       # 训练好的CNN模型（可选，仅在模板匹配不稳时启用）
└── tests/
    └── test_snapshot.py
```

配置和采集源均由主程序注入，本模块不单独维护配置文件。

---

## 5. 性能与精度目标（同 v2.0）

- **端到端延迟**：从收到新帧到生成快照 ≤ 500ms，可通过降低处理帧率平衡准确率。
- **初期识别错误率**：< 10%，后期通过模型迭代优化。
- **降级策略**：低置信度时，快照中 `confidence` 值反映真实水平，由主程序决定是否请求引擎返回近似建议。

---

## 6. 开发计划

| 阶段 | 任务 | 预计时间 | 产出 |
|------|------|---------|------|
| **一：接口对齐与骨架** | 定义 `PerceptionModule` 类、确认快照字典结构、编写 mock 供主程序测试 | 1 周 | 可被主程序调用的空模块 |
| **二：核心识别模块** | 开发底牌/公共牌模板匹配识别器、动作检测器 | 3 周 | 可输出手牌、公共牌、动作 |
| **三：数字识别与完整快照** | 集成 PaddleOCR 或数字分类器，填充底池、筹码、下注额等数字字段 | 2 周 | 完整 `game_snapshot` 输出 |
| **四：集成测试** | 与主程序联调，优化延迟与准确率 | 1 周 | 稳定运行版本 |
| **总计** | | **约 7 周** | |

---

## 7. 预算与硬件（保持不变）

- 核心硬件：已有 R5 5600 + 32G RAM + RTX3060，零额外成本。
- 采集卡（若选双机方案）：￥100-500（一次性）。
- 软件库均为开源。

---

## 8. 风险与应对

| 风险 | 应对 |
|------|------|
| ROI 依赖界面，更新后失效 | 主程序提供标定工具，可快速重新标定，无需改代码 |
| 识别置信度不足导致决策偏差 | 将置信度如实上报，主程序可拒绝查询或采用近似值 |
| 处理帧率过低导致延迟超标 | 主程序可动态调整 fps，或跳过非关键帧（如对手长考时） |

---
