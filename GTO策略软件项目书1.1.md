以下是可直接保存的《GTO 策略引擎模块》独立项目书。它完全剥离了屏幕感知与表现层，只专注于策略数据的生产、存储与查询接口，并预留了面向未来的调用接口。

---

# 《GTO 策略引擎模块》项目计划书

版本：1.0  
日期：2026-07-07  
作者：[你的名字]

---

## 1. 项目概述

### 1.1 项目定位
本项目是“个人 GTO 策略辅助系统”的核心策略引擎模块，负责提供翻后 GTO 策略的毫秒级查询服务。  
它不依赖任何特定的输入方式（手动自动网络）或显示界面，仅通过一套标准化接口对外输出策略数据，供上游程序（复盘软件、训练器、屏幕感知前端等）调用。

### 1.2 核心价值
- 独立运行：引擎本身是一个完整的黑盒服务，可脱离任何界面运行。
- 零延迟响应：所有查询均基于本地预计算数据库，无实时求解，响应时间  1ms。
- 接口清晰：提供 C++ API、命令行工具、本地 HTTPTCP 接口等多种调用方式。
- 数据自主：策略数据库自行生成，完全本地化，永不依赖外部订阅。

---

## 2. 技术架构

### 2.1 总体架构
```
┌──────────── 外部调用方 ────────────┐
│  复盘软件  训练器  感知前端      │
└──────────────┬────────────────────┘
               │ 调用标准接口
┌──────────────▼────────────────────┐
│         策略引擎 (核心 DLL服务)    │
│  ┌─────────────────────────────┐  │
│  │       查询调度层              │  │
│  │  解析请求 → 验证 → 路由       │  │
│  └──────────┬──────────────────┘  │
│             │                      │
│  ┌──────────▼──────────────────┐  │
│  │       索引与存储层           │  │
│  │  哈希索引 → mmap 节点文件   │  │
│  │  未命中处理  插值降级       │  │
│  └──────────┬──────────────────┘  │
│             │                      │
│  ┌──────────▼──────────────────┐  │
│  │     预计算策略数据库         │  │
│  │  节点文件 + 全局索引文件      │  │
│  └─────────────────────────────┘  │
└───────────────────────────────────┘
```

- 离线部分：使用 GTO+ 批量求解，生成标准化策略节点文件。
- 在线部分：策略引擎加载索引，提供查询 API。

### 2.2 关键技术选型
- 求解器：GTO+（永久许可，批量命令行求解）
- 数据编码：自定义二进制结构（8-bit 量化频率）或 FlatBuffers
- 索引：内存哈希表 + 文件偏移映射，启动时加载
- 核心语言：C++（性能），提供 C 接口以方便跨语言调用
- 可选服务接口：Python 绑定、命令行可执行文件、本地 HTTP 服务（用于多进程调用）

---

## 3. 接口设计（面向未来的扩展）

策略引擎对外暴露一套语言无关的查询协议，所有调用方只需构造标准请求，即可获得结构化策略结果。

### 3.1 通用请求格式
```json
{
  version 1.0,
  hand AhKh,            手牌，标准格式
  board As7d2h,         公共牌，可空
  history r200c,       行动序列（如 r200c 表示加注200后跟注）
  position BTN,         位置（BTNBBSB...）
  pot 1500,               当前底池（可选，用于EV计算）
  stack 8500,             有效筹码（可选）
  options {
    include_ev true,      是否返回EV
    include_tree false    是否返回子节点索引（用于树浏览）
  }
}
```

### 3.2 通用响应格式
```json
{
  status ok,             ok  approx  not_found
  node_id F184_042hash,
  actions [
    {
      type bet,
      size 0.33,           底池比例
      freq 0.85,
      ev 12.5
    },
    {
      type check,
      freq 0.15,
      ev 11.2
    }
  ],
  sub_nodes [              若请求 include_tree，返回下一层节点ID
    F184_042hash_child1,
    F184_042hash_child2
  ],
  is_exact true            是否精确命中
}
```

### 3.3 提供的具体接口形式

 接口形式  适用场景  说明 
------------------------
 CC++ 动态库  深度集成（同一进程）  `extern C char query(const char json_request);` 返回 JSON 字符串 
 命令行工具  脚本调用、测试  `strategy_engine.exe --query '{handAhKh,...}'` 输出 JSON 
 Python 绑定  Python 项目调用  `import gto_engine; result = gto_engine.query(...)` 
 本地 HTTP 服务  多进程、多语言调用  `POST http127.0.0.19090query` 收发 JSON，便于与感知模块分离 

这些接口的底层共享完全相同的查询逻辑，只是封装层不同。推荐优先实现 C++ 动态库和 Python 绑定，方便后续开发和集成。

---

## 4. 数据生产方案（离线部分）

### 4.1 求解参数
- 情境：BTN vs BB 单挑，现金局 100BB
- 翻牌抽象：184 类花色等价
- 转牌河牌：关键张单独求解，其余插值
- 下注尺度：33%、66%、全下
- 求解精度：0.5% 底池

### 4.2 批量生成流程（同前）
Python 脚本 → 调用 GTO+ 命令行 → 导出决策树文本 → 解析频率 → 量化编码 → 按 `{flop_class}{action_hash}.node` 存盘 → 生成全局索引文件。

### 4.3 数据输出
- 节点文件：每个决策点独立二进制文件，大小约数 KB。
- 索引文件：`index.bin`，包含所有节点的哈希→文件偏移映射，启动时一次性加载到内存（几 MB）。
- 元数据文件：描述求解参数、抽象版本等，便于校验。

预计总量：20–40 GB。

---

## 5. 引擎内部工作流程

1. 启动加载：读取索引文件，构建哈希表；打开策略数据库目录，准备 mmap。
2. 接收请求：标准化输入参数，计算翻牌抽象 ID 和行动序列哈希。
3. 精确查找：在哈希表中查找，命中则映射文件，读取节点结构体。
4. 未命中处理：若未命中，尝试近似节点（相同纹理类、忽略部分历史），并返回 `status approx`。
5. 结果封装：根据请求选项填充频率、EV、子节点等信息，输出 JSON。
6. 性能：整个流程（不含进程通信）在 0.01–1 ms 内完成。

---

## 6. 开发计划

 阶段  内容  时间 
------------------
 一：原型验证  5 翻牌手动求解，Python 解析并验证查询  1 周 
 二：数据管道  批量求解脚本，184 类翻牌，生成完整节点文件与索引  3 周 
 三：核心引擎 C++ 实现  哈希索引、mmap 读取、查询逻辑、未命中降级，输出标准 C 接口  2 周 
 四：接口封装  命令行工具、Python 绑定、本地 HTTP 服务（可选）  1 周 
 五：测试与优化  功能测试、边界测试、压力测试，封装成稳定动态库  1 周 
 总计   约 8 周 

---

## 7. 预算与资源

 项目  说明  预算 
------------------
 GTO+ 求解器  永久许可  ≈ $75 
 开发环境  C++ 编译器、Python、Qt（可选）  免费 
 存储  约 40GB  现有 
 总计   $75 

---

## 8. 风险与应对

 风险  应对 
------------
 数据生产时间过长  并行多机计算；降低抽象精度至 75 类 
 索引内存占用过大  使用紧凑数据结构或 LRU 缓存，非全量加载 
 二进制格式跨平台兼容  固定字节序，使用 FlatBuffers 等跨平台序列化库 

---

## 9. 后续集成方式

引擎模块开发完成后，可无缝对接至：
- 手工复盘软件：Qt 界面调用 DLL，构建完整决策树视图。
- 训练模拟器：沙盒对局时实时查询策略。
- 屏幕感知前端：通过本地 HTTP 或 Python 绑定，接收实时识别结果，返回策略建议。

所有集成方仅需遵循接口协议，无需了解策略库内部实现。

---

一句话总结：  
这是一个干净、独立、即刻可用的 GTO 策略查询黑盒，唯一职责就是接收牌局快照，返回精确的 GTO 混合策略。未来无论你要开发什么类型的上层应用，它都可以作为最稳定的决策核心。

---

保存方式：全选复制本文 → 粘贴到本地文本编辑器 → 保存为 `GTO策略引擎模块_项目书.md`，或用 Word 导出 PDF。